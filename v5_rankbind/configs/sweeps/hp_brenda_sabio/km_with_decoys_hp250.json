{
  "extends": "../../datasets/km_with_decoys.json",
  "name": "km_with_decoys_hp250",
  "description": "Stage-1 hard_pool_size sweep on BRENDA+SABIO km_with_decoys. hp=250 (other HPs unchanged from with_decoys default; bilinear head, margin loss, balanced sampler retained).",
  "triplet": {
    "hard_pool_size": 250,
    "negative_sampling_note": "Stage-1 sweep override: hard_pool_size=250. Diagnosed default=50 was 0.5\u20131.3 % of n_train_proteins on these scales (probe: null_prior_probe_brenda_sabio.csv); sweep covers ~4\u201350 % coverage."
  }
}