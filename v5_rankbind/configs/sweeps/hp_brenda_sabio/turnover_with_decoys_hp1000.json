{
  "extends": "../../datasets/turnover_with_decoys.json",
  "name": "turnover_with_decoys_hp1000",
  "description": "Stage-1 hard_pool_size sweep on BRENDA+SABIO turnover_with_decoys. hp=1000 (other HPs unchanged from with_decoys default; bilinear head, margin loss, balanced sampler retained).",
  "triplet": {
    "hard_pool_size": 1000,
    "negative_sampling_note": "Stage-1 sweep override: hard_pool_size=1000. Diagnosed default=50 was 0.5\u20131.3 % of n_train_proteins on these scales (probe: null_prior_probe_brenda_sabio.csv); sweep covers ~4\u201350 % coverage."
  }
}